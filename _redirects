# ============================================================
#  Cloudflare Pages - REDIRECTS - NÃO PODE HAVER LOOP OU REDIRECT AUTH
#  SE ALTERAR ALGO AQUI, TESTE LOGIN NA HORA.
# ============================================================

# --- 1) Landing (HOME institucional) - ACESSA EXPLICITAMENTE /landing.html ---
/landing                   /landing.html                200

# --- 2) ROOT (/) - só serve landing SE NÃO tiver query string de auth.
#        (se acessar raiz sem estar logado = landing. Logar vem de /auth/login.)
/                         /landing.html                200

# --- 3) DASHBOARD SPA - /index.html SEMPRE VAI PARA O PAINEL, NÃO PASSA POR LANDING.
#        (SEMPRE ACIMA DE QUALQUER REGRA DE RAIZ / PARA EVITAR LOOP!)
/index.html               /index.html                  200
/dashboard                /index.html                  200
/painel                   /index.html                  200
/home                     /index.html                  200
/app                      /index.html                  200

# --- NENHUMA OUTRA REGRA. Arquivos auth/ existem fisicamente e servem DIRETO.
# Qualquer outra URL = 404 padrão Pages (não temos catch-all / spa fallback).

