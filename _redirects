# ============================================================
#  Cloudflare Pages - REDIRECTS - VERSÃO MINIMA ZERO RISCO DE LOOP
#  NÃO TEMOS NENHUMA REGRA DE REDIRECT DE AUTH AQUI.
#  Todos os links de auth são compartilhados DIRETAMENTE com .html
#  Ex: /auth/register.html?ref=username   (não /auth/register)
# ============================================================

# --- ROOT (/) -> Landing Page Institucional Premium (landing.html) ---
/                         /landing.html                200

# --- /dashboard /painel /home -> Dashboard SPA index.html (usuarios logados) ---
/dashboard                /index.html                  200
/painel                   /index.html                  200
/home                     /index.html                  200
/app                      /index.html                  200

# --- NENHUMA OUTRA REGRA. NÃO TEM CATCH-ALL. NÃO TEM REDIRECT AUTH. ---
# (Qualquer outra URL = 404 padrão Pages.
#  Nossos arquivos auth existem fisicamente: /auth/register.html, /auth/login.html etc
#  Pages servem DIRETO sem redirect. NÃO TEM COMO DAR LOOP.)
